rules_version = '2';
// 각 사용자는 자기 폴더(users/{본인uid}/**)의 파일만 읽고 쓸 수 있다. 그 외 전부 차단.
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
